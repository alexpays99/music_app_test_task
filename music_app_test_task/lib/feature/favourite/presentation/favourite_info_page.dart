import 'package:flutter/material.dart';

class FavouriteInfoPage extends StatelessWidget {
  const FavouriteInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Favourite Info'),
      ),
    );
  }
}
